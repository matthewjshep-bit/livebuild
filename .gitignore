node_modules/
.next/
out/
# Anchored, because unanchored it matches a `build` directory at any depth -
# which quietly swallowed src/lib/build/ and would have left the shared build
# pipeline out of the repository with no warning.
/build/
.DS_Store
*.pem
.env*
!.env.example
next-env.d.ts
tsconfig.tsbuildinfo

# pipeline
pipeline/.venv/
pipeline/out/
__pycache__/
*.pyc

# Verification screenshots are build output, not source.
shots/
